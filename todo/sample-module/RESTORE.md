# Sample Module — Restore Guide

Move each file back to its original path exactly as listed below.
No services/types/hooks were specific to this module — nothing else to restore.

---

## Pages → `src/app/(pages)/(protected)/sample/`

| File in todo | Restore to |
|---|---|
| `pages/page.tsx` | `src/app/(pages)/(protected)/sample/page.tsx` |

---

## Notes

- This is a demo/showcase page with no backend integration.
- Uses shared layout components (`PageHeader`, `PageContainer`) and standard React hooks only.
