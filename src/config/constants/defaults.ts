/** Default value for the `reason` field on active records. */
export const DEFAULT_ACTIVE_REASON = 'active' as const

/** Default value for the `isActive` field on new records. */
export const DEFAULT_IS_ACTIVE = true as const

/** Default page size for paginated lists (matches paginationPageSize prop used in DataTable). */
export const DEFAULT_PAGE_SIZE = 20 as const
