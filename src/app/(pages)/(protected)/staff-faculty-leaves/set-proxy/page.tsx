"use client";

import { SetProxyPage } from "./_components/SetProxyPage";

/** Angular `staff-faculty-leaves/set-proxy` (StaffWorkloadComponent). */
export default function Page() {
  return <SetProxyPage />;
}
