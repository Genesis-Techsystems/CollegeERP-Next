'use client'

import { AddTimingSetPage } from './AddTimingSetPage'

/** Angular `timing-slots` — add / edit timing set with weekday grid and period slots. */
export function TimingSlotsPage() {
  return <AddTimingSetPage />
}
