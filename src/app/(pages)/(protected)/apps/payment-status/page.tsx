"use client";

import { PayStatusPage } from "@/app/(pages)/(protected)/student-fees/fees-due/_components/PayStatusPage";

/** Angular `apps/payment-status` — PayPhi / gateway return. */
export default function AppsPaymentStatusPage() {
  return <PayStatusPage />;
}
