import { redirect } from "next/navigation";

/** Alias → Angular Reports URL. */
export default function ReEvaluationComparisionReportAliasPage() {
  redirect("/reports/admin-exam-reports/re-evaluation-comparision-report");
}
