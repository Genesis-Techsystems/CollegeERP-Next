import { redirect } from "next/navigation";

/** Alias → Angular Reports URL. */
export default function ReEvaluationExamReportAliasPage() {
  redirect("/reports/admin-exam-reports/re-evaluation-exam-report");
}
