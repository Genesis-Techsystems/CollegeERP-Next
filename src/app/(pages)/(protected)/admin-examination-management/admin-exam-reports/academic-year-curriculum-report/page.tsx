import { redirect } from "next/navigation";

/** Alias → Angular Reports URL. */
export default function AcademicYearCurriculumReportAliasPage() {
  redirect("/reports/admin-exam-reports/academic-year-curriculum-report");
}
