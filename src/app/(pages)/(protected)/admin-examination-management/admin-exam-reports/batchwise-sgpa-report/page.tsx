import { redirect } from "next/navigation";

/** Alias → Angular Reports URL. */
export default function BatchWiseSgpaReportAliasPage() {
  redirect("/reports/admin-exam-reports/batchwise-sgpa-report");
}
