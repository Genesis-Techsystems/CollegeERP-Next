import { redirect } from "next/navigation";

/** Angular path alias → Reports fee-reports page. */
export default function ScholarshipPreceedingsAccountsAliasPage() {
  redirect("/reports/admin-fee-reports/scholarship-preceedings");
}
