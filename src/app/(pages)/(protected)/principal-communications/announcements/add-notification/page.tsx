"use client";

import { AddNotificationPage } from "@/app/(pages)/(protected)/notifications-and-announcements/_components/AddNotificationPage";

/**
 * Angular `#/principal-communications/announcements/add-notification`
 * (also reachable from emp-notifications Add button via send-notifications path)
 */
export default function PrincipalCommunicationsAnnouncementsAddPage() {
  return <AddNotificationPage />;
}
