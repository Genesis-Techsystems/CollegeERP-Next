import { PerformanceAssessmentPage } from '@/app/(pages)/(protected)/hr-payroll/_components/PerformanceAssessmentPage'

/** Angular `staff-faculty-details/performance-assessment` (principal / HOD faculty module). */
export default function Page() {
  return <PerformanceAssessmentPage />
}
