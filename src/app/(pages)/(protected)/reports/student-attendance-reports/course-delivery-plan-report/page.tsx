import { redirect } from "next/navigation";

/** Angular route alias → React page. */
export default function CourseDeliveryPlanReportAliasPage() {
  redirect("/reports/admin-attendance-reports/course-delivery-plan");
}
