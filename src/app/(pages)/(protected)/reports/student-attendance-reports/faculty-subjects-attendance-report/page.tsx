import { redirect } from "next/navigation";

export default function FacultySubjectsAttendanceReportAliasPage() {
  redirect(
    "/reports/admin-attendance-reports/faculty-subjects-attendance-report",
  );
}
