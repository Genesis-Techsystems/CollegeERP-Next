import { redirect } from "next/navigation";

/** Angular route alias → React page. */
export default function StudentPhotoSignatureReportAliasPage() {
  redirect("/reports/admin-student-reports/student-photo-signature-report");
}
